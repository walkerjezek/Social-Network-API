const { User, Thought, Reaction } = require('../models');

module.exports = {
    // get all users
    // get singel user
    // create a user
    // update a user by id
    // delete a user by id
    // add a friend
    // delete a friend from user

}